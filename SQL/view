CREATE VIEW vw_LowStockProducts AS
    SELECT ProductID,
           ProductName,
           Category,
           Quantity,
           MinStock,
           (MinStock - Quantity) AS ShortfallQty
    FROM   Products
    WHERE  Quantity <= MinStock;
